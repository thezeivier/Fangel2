import React from 'react';
import Wrapper from './../general/Wrapper'
import { TitleStyled, TextStyled } from './../../themes/internalRecyclableStyles'

const MainSettings = () => {
  return (
    <main>
      <Wrapper>
        <TitleStyled>Configuración</TitleStyled>
      </Wrapper>
    </main>
  );
}

export default MainSettings;
